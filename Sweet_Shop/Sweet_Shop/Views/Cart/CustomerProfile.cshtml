@model List<(int,Sweet_Shop.Models.Notification)>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order History</title>
    <style>
        body {
            font-family: Fantasy, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        .shopping-cart-container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            text-align: center;
            color: #54352b;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            border-bottom: 1px solid #ddd;
            padding: 10px 0;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

            li:last-child {
                border-bottom: none;
            }

        p {
            margin: 0;
            color: #54352b;
        }

        .btn {
            padding: 5px 10px;
            background-color: #54352b;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
            text-decoration: none;
        }

            .btn:hover {
                background-color: #321f18;
            }

        a {
            color: #007bff;
            text-decoration: none;
        }

            a:hover {
                text-decoration: underline;
            }
    </style>
</head>
<body>
    <div class="shopping-cart-container">

        <h2>Notifications</h2>
        @if (Model.Any())
        {
            <ul>
                @foreach (var tuple in Model)
                {
                    var notification = tuple.Item2;

                    <li>
                        <p>Notification: @notification.Message</p> <!-- Accessing the Message property -->
                    </li>
                }
            </ul>
        }
        else
        {
            <p>No new notifications</p>
        }

        <h2>Order History</h2>

        <ul>
            @if (Model.Any()){

            @foreach (var orderId in Model)
            {
                <li>
                    <p>Order Number: @orderId</p>
                    <a href="@Url.Action("OrderDetails", "Cart", new { id = orderId })" class="btn">CLICK HERE FOR DETAILS</a>
                </li>
                }
            }
            else
            {
                <p></p>
                <p>You haven't placed an order with us yet.</p>

            }
        </ul>
    </div>
</body>
</html>

@* @model List<int>


<p>Order History:</p>
<ul>
    @foreach (var orderId in Model)
    {
        <p>ordernumber: @orderId</p>
        <a href="@Url.Action("OrderDetails", "Cart", new { id = orderId })" class="btn btn-primary">CLICK HERE FOR DETAILS</a>

    }
</ul> *@

@* model List<Order>

<h1>Customer Profile</h1>

<h2>Order History</h2>

@if (Model != null && Model.Count() > 0)
{
    <table>
        <tr>
            <th>Order ID</th>
            <th>Product ID</th>
            <th>Quantity</th>
            <th>Order Date</th>
        </tr>
        @foreach (var order in Model)
        {
            <tr>
                <td>@order.OrdersId</td>
                <td>@order.productId</td>
                <td>@order.Quantity</td>
                <td>@order.OrderDate.ToString("yyyy-MM-dd HH:mm:ss")</td>
            </tr>
        }
    </table>
}
else
{
    <p>No orders found for this customer.</p>
}
 *@



@*@model List<Order>

<h1>Customer Profile</h1>

<h2>Order History</h2>

@if (Model != null && Model.Count() > 0)
{
    @foreach (var order in Model)
    {
        <h3>Order ID: @order.OrdersId</h3>
        @foreach (var order in order.OrdersId)
        {
            <tr>
                <td>@order.OrdersId</td>
                <td>@order.productId</td>
                <td>@order.Quantity</td>
                <td>@order.OrderDate.ToString("yyyy-MM-dd HH:mm:ss")</td>
            </tr>
        }
        
    }
}
else
{
    <p>No orders found for this customer.</p>
}*@
